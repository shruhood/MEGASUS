# Plugins

This directory is intended for custom user plugins.

Plugins extend the functionality of the MEGASUS toolkit without modifying the core codebase. Add your custom Python scripts here to have them automatically loaded by the plugin manager.
