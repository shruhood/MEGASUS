# Dashboard

This directory contains the web-based dashboard for the MEGASUS toolkit.

## Structure

- **`static/`**: Static assets such as CSS stylesheets, JavaScript files, and images used by the dashboard.
- **`templates/`**: HTML templates rendered by the backend to serve the web interface.
